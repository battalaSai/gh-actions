name: Deploy website
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v6
      - name: Install dependencies
        run: | 
          cd tasks/first-task/
          npm ci
      - name: Lint code
        run: |
          cd tasks/first-task/
          npm run lint
      - name: Test code
        run: |
          cd tasks/first-task/
          npm run test
  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v6
      - name: Install dependencies
        run: |
          cd tasks/first-task/
          npm ci
      - name: Build website
        run: |
          cd tasks/first-task/
          npm run build
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: echo "Deploying..."
    
